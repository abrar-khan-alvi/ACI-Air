# Route view components

Next.js routing is defined by the `app/` directory. The files here contain the
existing full-page visual implementations used by those App Router entries.
They are kept separate so route metadata and server concerns stay in `app/`
while interactive page behavior remains in explicit client components.
